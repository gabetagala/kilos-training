# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Environment — never commit real credentials (keep the template)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code project files — share workflow commands + project settings,
# ignore personal/local settings, skills, agents, and MCP config.
.claude/*
!.claude/commands/
!.claude/settings.json
.vercel

# Test + build artifacts
test-results/
playwright-report/
playwright/.cache/
coverage/

# Scratch + local agent/MCP tooling — never commit
.scratch/
.agents/
.mcp.json
.playwright-mcp/

# Stray root screenshots (real icons live in public/, not the repo root)
/*.png
